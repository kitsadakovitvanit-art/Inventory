# Copy to .env for local overrides (optional — leave unset for local dev,
# Vite's dev server proxy handles it automatically).

# Your deployed backend's URL. Set this in Vercel's project environment
# variables when you deploy.
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
