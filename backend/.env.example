# Copy to .env for local overrides (optional — defaults work fine for dev)

# Port the API server listens on. Render sets this automatically in production.
PORT=4000

# Your deployed frontend's URL, used to restrict CORS/WebSocket access.
# Leave unset in local dev to allow any origin.
# Example: FRONTEND_URL=https://your-app.vercel.app
FRONTEND_URL=
